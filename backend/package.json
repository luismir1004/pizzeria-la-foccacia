{
  "name": "backend",
  "version": "1.0.0",
  "description": "API de la Pizzería La Foccacia (Express + Prisma + PostgreSQL)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "seed": "node prisma/seed.js",
    "db:reset": "prisma migrate reset --force",
    "test": "node --test"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "keywords": [
    "pizzeria",
    "express",
    "prisma",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^7.3.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "prisma": "^7.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
