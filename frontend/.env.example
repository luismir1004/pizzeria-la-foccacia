# Copia a .env para desarrollo local del frontend.

# URL base de la API del backend.
# Si se deja vacío, el sitio usa el catálogo local (products.json) como fallback.
# Ejemplo en producción: VITE_API_URL=https://api.lafoccacia.com
VITE_API_URL=
